const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');
test('origen IA se marca y se limpia para el siguiente presupuesto',()=>{const source=fs.readFileSync('js/app.v3.6.3.js','utf8');const badge={innerHTML:''},window={};const ctx={window,document:{getElementById:id=>id==='pp-origen-ia'?badge:null}};vm.runInNewContext(source.slice(source.indexOf('function iaPresupuestoBadge'),source.indexOf('function iaReglasInstalacion')),ctx);ctx.iaMarcarPresupuesto(true);assert.equal(window._pptoCreadoConIA,true);assert.match(badge.innerHTML,/Creado con IA/);ctx.iaMarcarPresupuesto(false);assert.equal(window._pptoCreadoConIA,false);assert.equal(badge.innerHTML,'');});
test('dictado conserva texto anterior y no envía automáticamente',()=>{let r;const input={value:'Prepará',dispatchEvent(){}},button={setAttribute(){}};const window={SpeechRecognition:function(){r=this;this.start=()=>{};this.abort=()=>{this.onend();};}};vm.runInNewContext(fs.readFileSync('js/modules/ai-learning.js','utf8'),{window,document:{addEventListener(){},getElementById:id=>id==='ia-input'?input:button},Event:function(){},notify(){}});window.svIaLearning.dictate();r.onresult({results:[[{transcript:'un presupuesto'}]]});assert.equal(input.value,'Prepará un presupuesto');window.svIaLearning.stop();assert.equal(button.textContent,'Dictar');});
