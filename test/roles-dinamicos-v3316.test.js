const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');
test('Roles usa nombres del menú y orden alfabético',()=>{const s=require('./helpers/active-app').readActiveApp().source,start=s.indexOf('function obtenerTodosModulosRoles('),end=s.indexOf('\nfunction ',start+10);const c={TODOS_MODULOS:[{id:'soporte',label:'Soporte'},{id:'agenda',label:'Agenda'}],document:{querySelectorAll:()=>[{getAttribute:()=>"showPage('soporte')",textContent:'Soporte / Reclamos'}]}};vm.createContext(c);vm.runInContext(s.slice(start,end),c);const list=c.obtenerTodosModulosRoles();assert.equal(list[0].label,'Agenda');assert.equal(list[1].label,'Soporte / Reclamos');});
test('el técnico respeta habilitación y bloqueo configurados sin veto adicional',()=>{const w={currentRole:'tecnico',PERMISOS_ROLES:{tecnico:{bloqueados:[]}}};vm.runInNewContext(fs.readFileSync('js/core/access-control.js','utf8'),{window:w});assert.equal(w.SisVentas.Access.resolvePage('venta').redirected,false);w.PERMISOS_ROLES.tecnico.bloqueados=['venta'];assert.equal(w.SisVentas.Access.resolvePage('venta').redirected,true);});
