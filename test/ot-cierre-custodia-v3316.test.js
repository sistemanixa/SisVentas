const test=require('node:test'),assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs');
test('materiales sin entrega o sin rendición bloquean cierre; rendición completa habilita',()=>{const c={window:{prodData:{}},document:{}};vm.createContext(c);vm.runInContext(fs.readFileSync('js/modules/ot-material-custody.js','utf8'),c);const validate=c.window.otCustodiaValidarCierre;const m={cod:'C',desc:'Cable canal',vendida:2};assert.equal(validate({materiales:[m]}).ok,false);assert.equal(validate({materiales:[m],custodiaIniciada:true}).ok,false);assert.equal(validate({materiales:[]}).ok,true);});
