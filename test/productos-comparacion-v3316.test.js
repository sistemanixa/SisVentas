const test=require('node:test'),assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs');
const s=require('./helpers/active-app').readActiveApp().source;const c={URL,escapeHTML:x=>String(x).replaceAll('&','&amp;').replaceAll('"','&quot;'),completarReferenciaProveedorProducto:p=>p};vm.createContext(c);for(const fn of ['ordenarProveedoresComparacion','enlaceUrlProveedorProducto']){const a=s.indexOf('function '+fn+'('),b=s.indexOf('\nfunction ',a+10);vm.runInContext(s.slice(a,b),c);}
test('ordena por costo comparable y deja sin precio al final sin cambiar el registro',()=>{const lista=[{precio:300},{precio:0},{precio:100,costoRealArs:121},{precio:110,costoRealArs:110}];const antes=JSON.stringify(lista);assert.deepEqual(Array.from(c.ordenarProveedoresComparacion(lista),r=>r.costo),[110,121,300,0]);assert.equal(JSON.stringify(lista),antes);});
test('enlace abre la URL específica y rechaza protocolos ejecutables',()=>{assert.match(c.enlaceUrlProveedorProducto('https://clientes.ciardi.com.ar/producto.php?p=4100975','Ciardi'),/target="_blank"/);assert.doesNotMatch(c.enlaceUrlProveedorProducto('javascript:alert(1)','Proveedor'),/<a /);assert.doesNotMatch(c.enlaceUrlProveedorProducto('','Proveedor'),/<a /);});
